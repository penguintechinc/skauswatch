"""Minimal fixtures for smoke tests."""

import os
import sys

# Root of the repository (two levels up from tests/smoke/)
REPO_ROOT = os.path.abspath(os.path.join(os.path.dirname(__file__), "..", ".."))
