apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: redis-netpol
  namespace: darwin
  labels:
    app: darwin
    component: network-policy
    service: redis
spec:
  podSelector:
    matchLabels:
      app: redis
  policyTypes:
    - Ingress
    - Egress
  ingress:
    # Allow traffic from flask-backend pods only
    - from:
        - podSelector:
            matchLabels:
              app: flask-backend
      ports:
        - protocol: TCP
          port: 6379

  egress:
    # Deny all egress traffic (cache isolation)
    - to: []
