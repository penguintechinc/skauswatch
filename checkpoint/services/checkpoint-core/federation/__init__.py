"""
checkpoint-core — upstream IDP federation.

Sub-modules:
  federation.sync   — UpstreamSyncLoop: batch user import from upstream IDPs
  federation.proxy  — ProxyDelegate: JIT user provisioning via upstream auth delegation
"""
