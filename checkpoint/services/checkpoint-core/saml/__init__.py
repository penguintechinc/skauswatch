"""
checkpoint-core — SAML 2.0 IdP implementation.

Provides SAML assertion building, validation, and SP-initiated SSO.

Sub-modules:
  saml.utils      — build_saml_response, validate_saml_response, generate_saml_request
  saml.endpoints  — Blueprint: saml_bp, prefix /saml
"""
