apiVersion: apps/v1
kind: Deployment
metadata:
  name: icebox-pki-server
  namespace: icebox
  labels:
    app.kubernetes.io/name: icebox-pki-server
    app.kubernetes.io/component: pki-server
    app.kubernetes.io/part-of: icebox
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: icebox-pki-server
  template:
    metadata:
      labels:
        app.kubernetes.io/name: icebox-pki-server
    spec:
      securityContext:
        runAsNonRoot: true
        runAsUser: 1000
        allowPrivilegeEscalation: false
      containers:
        - name: pki-server
          image: icebox-pki-server:latest
          imagePullPolicy: Never
          ports:
            - name: http
              containerPort: 8081
          envFrom:
            - configMapRef:
                name: icebox-pki-server-config
            - secretRef:
                name: icebox-pki-server-secrets
          livenessProbe:
            httpGet:
              path: /healthz
              port: 8081
            initialDelaySeconds: 30
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /healthz
              port: 8081
            initialDelaySeconds: 5
            periodSeconds: 5
          resources:
            requests:
              cpu: 50m
              memory: 64Mi
            limits:
              cpu: 100m
              memory: 128Mi
