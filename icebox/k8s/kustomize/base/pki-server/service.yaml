apiVersion: v1
kind: Service
metadata:
  name: icebox-pki-server
  namespace: icebox
  labels:
    app.kubernetes.io/name: icebox-pki-server
    app.kubernetes.io/part-of: icebox
spec:
  type: ClusterIP
  ports:
    - port: 8081
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app.kubernetes.io/name: icebox-pki-server
