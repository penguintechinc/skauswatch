apiVersion: v1
kind: Service
metadata:
  name: icebox-ssh-ca
  namespace: icebox
  labels:
    app.kubernetes.io/name: icebox-ssh-ca
    app.kubernetes.io/part-of: icebox
spec:
  type: ClusterIP
  ports:
    - port: 8082
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app.kubernetes.io/name: icebox-ssh-ca
