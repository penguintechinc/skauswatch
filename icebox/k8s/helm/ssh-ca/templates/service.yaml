apiVersion: v1
kind: Service
metadata:
  name: {{ include "icebox-ssh-ca.fullname" . }}
  labels:
    {{- include "icebox-ssh-ca.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "icebox-ssh-ca.selectorLabels" . | nindent 4 }}
