apiVersion: v1
kind: Service
metadata:
  name: {{ include "icebox-pki-server.fullname" . }}
  labels:
    {{- include "icebox-pki-server.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "icebox-pki-server.selectorLabels" . | nindent 4 }}
