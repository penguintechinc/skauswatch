apiVersion: v1
kind: Service
metadata:
  name: {{ include "icebox-flask-backend.fullname" . }}
  labels:
    {{- include "icebox-flask-backend.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "icebox-flask-backend.selectorLabels" . | nindent 4 }}
