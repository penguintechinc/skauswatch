apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "skauswatch-codescan-backend.fullname" . }}-config
  labels:
    {{- include "skauswatch-codescan-backend.labels" . | nindent 4 }}
  namespace: {{ .Release.Namespace }}
data:
  {{- toYaml .Values.configMapData | nindent 2 }}
