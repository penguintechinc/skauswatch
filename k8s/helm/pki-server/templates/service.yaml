apiVersion: v1
kind: Service
metadata:
  name: {{ include "skauswatch-pki-server.fullname" . }}
  labels:
    {{- include "skauswatch-pki-server.labels" . | nindent 4 }}
  namespace: {{ .Release.Namespace }}
  {{- with .Values.service.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "skauswatch-pki-server.selectorLabels" . | nindent 4 }}
