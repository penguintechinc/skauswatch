apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "skauswatch-pki-server.fullname" . }}-config
  labels:
    {{- include "skauswatch-pki-server.labels" . | nindent 4 }}
  namespace: {{ .Release.Namespace }}
data:
  {{- toYaml .Values.configMapData | nindent 2 }}
