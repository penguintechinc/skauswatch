apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "skauswatch-aaa-monitor.fullname" . }}-config
  labels:
    {{- include "skauswatch-aaa-monitor.labels" . | nindent 4 }}
  namespace: {{ .Release.Namespace }}
data:
  {{- toYaml .Values.configMapData | nindent 2 }}
