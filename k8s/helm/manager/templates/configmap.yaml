apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "skauswatch-manager.fullname" . }}-config
  labels:
    {{- include "skauswatch-manager.labels" . | nindent 4 }}
  namespace: {{ .Release.Namespace }}
data:
  {{- toYaml .Values.configMapData | nindent 2 }}
