apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "skauswatch-worker-codescan.fullname" . }}-config
  labels:
    {{- include "skauswatch-worker-codescan.labels" . | nindent 4 }}
  namespace: {{ .Release.Namespace }}
data:
  {{- toYaml .Values.configMapData | nindent 2 }}
