apiVersion: v1
kind: Secret
metadata:
  name: {{ include "skauswatch-s3scan.fullname" . }}-secret
  labels:
    {{- include "skauswatch-s3scan.labels" . | nindent 4 }}
  namespace: {{ .Release.Namespace }}
type: Opaque
data:
  {{- range $key, $value := .Values.secretData }}
  {{ $key }}: {{ $value | b64enc | quote }}
  {{- end }}
