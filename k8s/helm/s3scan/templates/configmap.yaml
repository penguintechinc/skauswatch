apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "skauswatch-s3scan.fullname" . }}-config
  labels:
    {{- include "skauswatch-s3scan.labels" . | nindent 4 }}
  namespace: {{ .Release.Namespace }}
data:
  {{- toYaml .Values.configMapData | nindent 2 }}
