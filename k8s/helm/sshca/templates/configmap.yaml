apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "skauswatch-sshca.fullname" . }}-config
  labels:
    {{- include "skauswatch-sshca.labels" . | nindent 4 }}
  namespace: {{ .Release.Namespace }}
data:
  {{- toYaml .Values.configMapData | nindent 2 }}
