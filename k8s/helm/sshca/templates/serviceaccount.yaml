{{- if .Values.serviceAccount.create }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "skauswatch-sshca.serviceAccountName" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "skauswatch-sshca.labels" . | nindent 4 }}
  {{- with .Values.serviceAccount.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
{{- end }}
