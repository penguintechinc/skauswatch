apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "skauswatch-webui.fullname" . }}-config
  labels:
    {{- include "skauswatch-webui.labels" . | nindent 4 }}
  namespace: {{ .Release.Namespace }}
data:
  {{- toYaml .Values.configMapData | nindent 2 }}
