apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "skauswatch-worker-s3.fullname" . }}-config
  labels:
    {{- include "skauswatch-worker-s3.labels" . | nindent 4 }}
  namespace: {{ .Release.Namespace }}
data:
  {{- toYaml .Values.configMapData | nindent 2 }}
