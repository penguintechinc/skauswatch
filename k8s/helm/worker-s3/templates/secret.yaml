apiVersion: v1
kind: Secret
metadata:
  name: {{ include "skauswatch-worker-s3.fullname" . }}-secret
  labels:
    {{- include "skauswatch-worker-s3.labels" . | nindent 4 }}
  namespace: {{ .Release.Namespace }}
type: Opaque
data:
  {{- range $key, $value := .Values.secretData }}
  {{ $key }}: {{ $value | b64enc | quote }}
  {{- end }}
