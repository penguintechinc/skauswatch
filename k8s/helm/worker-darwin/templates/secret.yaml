apiVersion: v1
kind: Secret
metadata:
  name: {{ include "skauswatch-worker-darwin.fullname" . }}-secret
  labels:
    {{- include "skauswatch-worker-darwin.labels" . | nindent 4 }}
  namespace: {{ .Release.Namespace }}
type: Opaque
stringData:
  {{- toYaml .Values.secretData | nindent 2 }}
