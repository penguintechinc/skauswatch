apiVersion: v1
kind: Secret
metadata:
  name: {{ include "skauswatch-edr-agent.fullname" . }}-secret
  labels:
    {{- include "skauswatch-edr-agent.labels" . | nindent 4 }}
  namespace: {{ .Release.Namespace }}
type: Opaque
stringData:
  {{- toYaml .Values.secretData | nindent 2 }}
