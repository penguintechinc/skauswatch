apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "skauswatch-edr-agent.fullname" . }}-config
  labels:
    {{- include "skauswatch-edr-agent.labels" . | nindent 4 }}
  namespace: {{ .Release.Namespace }}
data:
  {{- toYaml .Values.configMapData | nindent 2 }}
