apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "skauswatch-worker-scanner.fullname" . }}-config
  labels:
    {{- include "skauswatch-worker-scanner.labels" . | nindent 4 }}
  namespace: {{ .Release.Namespace }}
data:
  {{- toYaml .Values.configMapData | nindent 2 }}
