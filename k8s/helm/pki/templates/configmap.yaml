apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "skauswatch-pki.fullname" . }}-config
  labels:
    {{- include "skauswatch-pki.labels" . | nindent 4 }}
  namespace: {{ .Release.Namespace }}
data:
  {{- toYaml .Values.configMapData | nindent 2 }}
