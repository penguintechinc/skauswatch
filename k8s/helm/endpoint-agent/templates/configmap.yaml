apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "skauswatch-endpoint-agent.fullname" . }}-config
  labels:
    {{- include "skauswatch-endpoint-agent.labels" . | nindent 4 }}
  namespace: {{ .Release.Namespace }}
data:
  {{- toYaml .Values.configMapData | nindent 2 }}
