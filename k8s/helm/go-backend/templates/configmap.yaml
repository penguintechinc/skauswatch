apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "go-backend.fullname" . }}-config
  labels:
    {{- include "go-backend.labels" . | nindent 4 }}
  namespace: {{ .Release.Namespace }}
data:
  {{- toYaml .Values.configMapData | nindent 2 }}
