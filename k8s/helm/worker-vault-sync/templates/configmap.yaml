apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "skauswatch-worker-vault-sync.fullname" . }}-config
  labels:
    {{- include "skauswatch-worker-vault-sync.labels" . | nindent 4 }}
  namespace: {{ .Release.Namespace }}
data:
  {{- toYaml .Values.configMapData | nindent 2 }}
