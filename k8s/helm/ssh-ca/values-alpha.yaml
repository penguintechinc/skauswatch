# Alpha environment overrides (local MicroK8s)
replicaCount: 1

image:
  pullPolicy: Never
  tag: latest

namespace: skauswatch

env:
  - name: LOG_LEVEL
    value: "DEBUG"
  - name: FLASK_ENV
    value: "development"
  - name: RELEASE_MODE
    value: "false"
  # IceBox shim: forward to IceBox ssh-ca (absorbed service)
  - name: ICEBOX_SSH_CA_URL
    value: "http://icebox-ssh-ca.icebox.svc.cluster.local:8082"

autoscaling:
  enabled: false

resources:
  limits:
    cpu: 250m
    memory: 256Mi
  requests:
    cpu: 50m
    memory: 128Mi

# Internal service only - no ingress
ingress:
  enabled: false
