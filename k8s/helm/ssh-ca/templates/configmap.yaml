apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "skauswatch-ssh-ca.fullname" . }}-config
  labels:
    {{- include "skauswatch-ssh-ca.labels" . | nindent 4 }}
  namespace: {{ .Release.Namespace }}
data:
  {{- toYaml .Values.configMapData | nindent 2 }}
