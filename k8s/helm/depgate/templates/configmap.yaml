apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "skauswatch-depgate.fullname" . }}-config
  labels:
    {{- include "skauswatch-depgate.labels" . | nindent 4 }}
  namespace: {{ .Release.Namespace }}
data:
  {{- toYaml .Values.configMapData | nindent 2 }}
