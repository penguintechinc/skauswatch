apiVersion: v1
kind: Secret
metadata:
  name: {{ include "skauswatch-scanner.fullname" . }}-secret
  labels:
    {{- include "skauswatch-scanner.labels" . | nindent 4 }}
  namespace: {{ .Release.Namespace }}
type: Opaque
stringData:
  {{- toYaml .Values.secretData | nindent 2 }}
