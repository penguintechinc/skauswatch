apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "skauswatch-logs.fullname" . }}-config
  labels:
    {{- include "skauswatch-logs.labels" . | nindent 4 }}
  namespace: {{ .Release.Namespace }}
data:
  {{- toYaml .Values.configMapData | nindent 2 }}
