# Default (prod-safe baseline) values for skauswatch-logs.
# Per-environment overrides live in alpha.yml / beta.yml / gamma.yml / production.yml.
replicaCount: 3

image:
  repository: ghcr.io/penguintechinc/skauswatch/logs
  pullPolicy: IfNotPresent
  tag: "latest"

imagePullSecrets: []
nameOverride: ""
fullnameOverride: ""

# Name of a pre-provisioned Secret (or ExternalSecret target) to use
# instead of the chart-managed one below. When set, templates/secret.yaml
# is not rendered and Deployment/DaemonSet env vars read from this Secret.
existingSecret: ""

serviceAccount:
  create: true
  annotations: {}
  name: ""

podAnnotations: {}

podSecurityContext:
  runAsNonRoot: true
  runAsUser: 10001  # FIRST-RUN FIX (k8s-ops): matches Dockerfile useradd --uid 10001 "appuser"; was 1000 (mismatch broke pki's ssh-keygen subprocess: "No user exists for uid 1000")
  fsGroup: 10001
  # Required for Pod Security Admission "restricted" compliance (and
  # good practice under "baseline") -- see k8s/helm/README.md "Cluster
  # security baseline".
  seccompProfile:
    type: RuntimeDefault

securityContext:
  allowPrivilegeEscalation: false
  capabilities:
    drop:
      - ALL
  readOnlyRootFilesystem: true

# Tetragon exec-allowlist enforcement (templates/tracingpolicy.yaml).
# Requires the Tetragon agent DaemonSet in-cluster -- set false to render
# without it (e.g. a cluster that hasn't rolled Tetragon out yet).
tetragon:
  enabled: true

httpPort: 5010
metricsPort: 9090

service:
  type: ClusterIP
  port: 5010
  metricsPort: 9090
  annotations: {}

ingress:
  enabled: false
  className: "nginx"
  annotations: {}
  hosts:
    - host: skauswatch.app
      paths:
        - path: /api/logs
          pathType: Prefix
  tls: []

httproute:
  enabled: false
  gatewayName: shared
  gatewayNamespace: gateway
  host: skauswatch.app
  rules:
    - path: /api/logs
      service: skauswatch-logs
      port: 5010

resources:
  limits:
    cpu: 1000m
    memory: 1Gi
  requests:
    cpu: 500m
    memory: 512Mi

autoscaling:
  enabled: true
  minReplicas: 3
  maxReplicas: 10
  targetCPUUtilizationPercentage: 70
  targetMemoryUtilizationPercentage: 80

nodeSelector: {}

tolerations: []

affinity: {}

# services/logs/src/config.rs reads OPENSEARCH_URL/LOG_RETENTION_DAYS/
# HTTP_PORT — no DB/license vars are wired up in this ingest path.
# JWT_VERIFY_KEY is required though: services/logs/src/main.rs and
# src/ingest.rs load it via skauswatch_auth::load_jwt_secret and require a
# valid ES256 bearer token (signed by manager, the sole issuer) on every
# /ingest request.
env:
  - name: HTTP_PORT
    value: "5010"
  - name: OPENSEARCH_URL
    valueFrom:
      configMapKeyRef:
        name: skauswatch-logs-config
        key: opensearch-url
  - name: LOG_RETENTION_DAYS
    valueFrom:
      configMapKeyRef:
        name: skauswatch-logs-config
        key: log-retention-days
  # PEM EC P-256 public key (SPKI) verifying ES256 tokens minted by
  # manager (the sole issuer) -- see k8s/helm/manager/values.yaml
  # secretData for keypair generation commands.
  - name: JWT_VERIFY_KEY
    valueFrom:
      secretKeyRef:
        name: '{{ include "skauswatch-logs.secretName" . }}'
        key: jwt-verify-key

# CHART FIX: services/logs/src/ingest.rs registers GET /healthz only — no
# /readyz route exists on this service (unlike depgate/s3scan/scanner/
# sshca/worker-codescan/worker-vault-sync/monitor, which all merge
# skauswatch_telemetry::health_router). Pointing readinessProbe at /readyz
# 404s permanently (pod never Ready, blocks Service endpoints + e2e) — until
# a /readyz route is added to services/logs, alias readiness to the
# endpoint that actually exists. Flagged for a follow-up code change.
livenessProbe:
  httpGet:
    path: /healthz
    port: http
  initialDelaySeconds: 10
  periodSeconds: 10
  timeoutSeconds: 5
  failureThreshold: 3

readinessProbe:
  httpGet:
    path: /healthz
    port: http
  initialDelaySeconds: 5
  periodSeconds: 5
  timeoutSeconds: 3
  failureThreshold: 2

volumeMounts: []

volumes: []

configMapData:
  opensearch-url: "http://opensearch:9200"
  log-retention-days: "90"

# MUST be set via --set/values or existingSecret; services fail closed
# on a blank/known-default secret (never ship a real credential here).
secretData:
  jwt-verify-key: ""
